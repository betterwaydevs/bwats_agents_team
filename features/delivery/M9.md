# M9: Delivery Log

## PM: Assignment
- **Status**: done
- **Agent**: project-manager
- **Date**: 2026-02-27
- **Notes**: Assigned to backend-developer + frontend-developer. Spec reviewed, all dependencies clear. Backend builds webhook receiver, unread tracking table, and query endpoints. Frontend builds notification dropdown in header.

## DEV: Backend
- **Status**: done
- **Agent**: backend-developer
- **Date**: 2026-02-27
- **Notes**:
  - Created `whatsapp_unread` table (ID 201) on both `development` and `v1` branches with indexes on person_id+person_type, unread_count, last_message_at
  - Deployed 7 endpoints to `messaging` API group (canonical `2CPT0xvS`) on both branches:
    - `POST /messaging/whatsapp_webhook` (ID 43110/43118) — Lightweight receiver: validates `WHAPI_WEBHOOK_SECRET`, extracts phone+message+from_name, upserts `whatsapp_unread`. No person lookup (deferred to GET).
    - `GET /messaging/whatsapp_webhook` (ID 43113/43119) — Health check for WHAPI validation
    - `PUT/DELETE/PATCH /messaging/whatsapp_webhook` — WHAPI multi-method validation passthrough
    - `GET /messaging/whatsapp_unread` (ID 43111/43123) — Lists unread notifications with lazy person matching by phone, returns from_name as fallback, backfills person_id/person_type on first access
    - `PUT /messaging/whatsapp_mark_read` (ID 43112/43124) — Resets unread_count and sets read_at
  - `whatsapp_unread` table has `from_name` TEXT column for WHAPI display name storage
  - Webhook accepts all WHAPI event types (messages, chats, contacts, groups, statuses) but only processes messages
  - WHAPI webhook validated and active on both dev and v1
  - Sender identification: person_name (DB match) > from_name (WhatsApp name) > phone_number
- **Commits**: bwats_xano@b6a5a56 (initial), pending commit (simplified webhook, added from_name, GET/PUT/DELETE/PATCH handlers, updated unread endpoint)

## DEV: Frontend
- **Status**: done
- **Agent**: frontend-developer
- **Date**: 2026-02-27
- **Notes**:
  - `WhatsAppNotificationDropdown.tsx` — MessageCircle icon with green badge, popover dropdown with unread messages, polls every 60s via react-query. Display priority: person_name > from_name (WhatsApp name) > phone_number. Guards against 404 navigation when person_slug is empty. Marks as read on click.
  - `whatsappNotificationApi.ts` — API service: `getWhatsAppUnread(token)` and `markWhatsAppRead(token, personId, personType)`. Uses existing `API_KEYS.emailMessaging` (`2CPT0xvS`).
  - `UserHeader.tsx` — Integrated dropdown in both desktop and mobile header, gated behind `!isProjectRestricted`
  - `CommunicationTab.tsx` — Added `?whatsapp=1` query param detection to auto-select WhatsApp sub-tab
  - `WhatsAppTab.tsx` — Added `useEffect` to call `markWhatsAppRead` when tab is viewed, invalidates `whatsappUnread` query to update badge
  - Navigation uses `#communication` hash (matches ProfileContent tab routing) + `?whatsapp=1` param
- **Commits**: nearshore-talent-compass@837aaa5 (initial), local changes pending commit (navigation fix, mark-as-read, auto-sub-tab, phone fallback)

## QA: Testing
- **Status**: done
- **Agent**: qa-tester
- **Date**: 2026-02-27
- **Notes**:
  - Playwright E2E test at `tests/whatsapp-notifications.spec.ts` — PASS (42s)
  - Verified: notification icon renders, dropdown opens with unread data, profile navigation works, Communication tab activates, WhatsApp sub-tab selects correctly
  - Issue found: test phone number doesn't match a real person, so notification shows phone instead of name — this is expected for unknown contacts
  - Backend endpoints verified: webhook returns `{"success":true}`, unread/mark_read endpoints respond with correct auth requirements
- **Screenshots**: 01-header-whatsapp-badge.png, 02-whatsapp-dropdown.png, 03-profile-communication-tab.png, 04-whatsapp-chat-tab.png
- **Report**: tests/reports/whatsapp-notification-report.md

## PO: Acceptance
- **Status**: in-progress
- **Agent**: product-owner
- **Date**: 2026-02-27
- **Notes**: Core flow works with test data. Pending real-world validation with a WhatsApp message from a phone matching a person in the database. WHAPI webhooks active on both dev and v1.

## User: Approval
- **Status**: pending
- **Date**:
- **Notes**:
