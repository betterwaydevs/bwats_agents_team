# QF14 — Add_prospect db.patch Type Error Fix — Delivery Report

> **Status**: dev-deployed | **Date**: 2026-03-03

## Summary

Fixed the `parse_unparsed_prospect` function (ID 11189) to handle AI parser output where array-typed fields (`skills`, `work_history`, `education`, `certifications`, `languages`, `industries`) arrive as JSON strings instead of parsed arrays. This eliminates the `db.patch` type error that was causing prospects to be incorrectly marked as `conflict` status.

## Changes

### Modified File
- `functions/11189_parse_unparsed_prospect.xs` — Added two-layer defensive parsing for array fields

### Change Details
1. **Early-stage parsing (line ~225)**: Pre-parses all 6 array fields on the `parsed` object before date/number normalization runs, so downstream processing still applies correctly
2. **Safety net before return (line ~309)**: Second defense layer on `dbData` right before `return dbData`, catching any edge case where a string might slip through after the copy

## Sign-offs

### DEV Sign-off
- **Agent**: backend-developer
- **Date**: 2026-03-02
- **Status**: PASS
- **Notes**: Root cause identified — the AI parser (`call_open_ia`) occasionally returns array fields as JSON strings (e.g., `"[{\"name\":\"Python\"}]"` instead of `[{"name":"Python"}]`). When `db.patch` receives a string for a column expecting an array, Xano's PHP layer throws a type error. The fix adds defensive JSON parsing at two points in the `api.lambda` that builds `$db_update_data`. The fix also handles edge cases: plain objects (not arrays) get wrapped in `[obj]`, unparseable strings default to `[]`.

### QA Sign-off (Real Execution — Limited)
- **Agent**: qa-tester
- **Date**: 2026-03-03
- **Time**: 12:35–12:36 UTC
- **Status**: PASS (limited — full runtime test blocked pending v1 merge)
- **Test Type**: Real curl execution + deployment verification
- **Test Results**:
  - [PASS] **AC1 — Root cause identified**: AI parser returns array fields (skills, work_history, education, certifications, languages, industries) as JSON strings. `db.patch` throws `DBO::set(): Argument #2 ($b) must be of type array, string given`.
  - [PASS] **AC2 — Fix deployed to dev**: Two-layer defensive parsing deployed to dev function ID 11189. Verified via MCP `getFunction` — code matches local file. XanoScript syntax: OK.
  - [BLOCKED] **AC3 — Reprocess prospect 72100**: Cannot test — dev function (ID 11189) is not callable via API endpoints (all on v1 branch). v1 function (ID 11280) does NOT have the fix.
  - [BLOCKED] **AC4 — No more type-error conflicts**: Requires v1 deployment and monitoring.
  - [PASS] **Endpoint accessibility**: `create_prospect_from_html` endpoint responded to real HTTP calls (returned 500 "Duplicate record detected" — app-level error confirming endpoint is alive and processing requests). Tested at 12:35:37 UTC (0.48s) and 12:36:43 UTC (0.58s).
  - [PASS] **function.run calls verified (via QF7)**: All 6 `function.run` calls in `parse_unparsed_prospect` (call_open_ia, elastic_search/document x3, prospect_quick_normalize_skills, agents/call_prospect_router) use correct syntax — verified working via QF7 sweep tests.
- **Report**: `features/reports/QF14/qf14-test-report.html`
- **Notes**: The fix is deployed and verified on dev. Full runtime testing (reprocessing prospect 72100, verifying no more type-error conflicts) is blocked because all API endpoints are on v1 branch and the dev function cannot be called directly. Next steps: (1) merge dev to v1, (2) reset prospect 72100 parse_status to "pending", (3) trigger reprocessing, (4) monitor for DBO::set() errors.

### PO Sign-off
- **Agent**: product-owner
- **Date**: 2026-03-03
- **Status**: PASS
- **Notes**: The fix directly addresses the root cause (AC1: identified as AI returning JSON strings for array fields, AC2: fixed with defensive parsing). The two-layer defense is appropriate given the unpredictable nature of AI parser output. AC3 (reprocess prospect 72100) and AC4 (no more type-error conflicts) require deployment verification. The fix is backwards-compatible — existing correctly-formatted data passes through unchanged.

### User: Approval
- **Status**: pending

## Deployment Log

### Dev Branch Deployment — 2026-03-03 11:57 UTC
- **Agent**: backend-developer
- **Method**: MCP `getFunction` with `X-Branch: dev` header → verified function ID 11189 on workspace 6 (BetterWayDevs)
- **Verification**: Remote XanoScript on dev branch matches local file exactly — both QF14 changes present:
  - Pre-parse block (line ~225): Parses string array fields on `parsed` object before normalization
  - Safety-net block (line ~309): Second defense on `dbData` before return
- **XanoScript status**: `ok` (no syntax errors)
- **Function updated_at**: 2026-03-03 11:14:20 UTC (already deployed before this verification)

### Runtime Test — 2026-03-03 11:58 UTC
- **v1 Add_prospect endpoint**: Called `GET /api:zE_czJ22/Add_prospect?auth_token=...`
- **Result**: `{"found":true,"id":73492,"status":"parsed","es_doc_id":"ZlyYs5wBYzrcWrEDHAaq","es_existed":false,"error":""}`
- **Prospect 73492**: Successfully parsed, new ES document created
- **Note**: This tested the v1 function (ID 11280), not the dev function (ID 11189). The QF14 bug is intermittent — only triggers when AI returns stringified JSON arrays. The v1 function does not have QF14 fix but happened to parse this prospect without hitting the bug.

### Dev Function Runtime Test Limitation
- **Issue**: The dev branch function (ID 11189) cannot be runtime-tested because:
  1. All API endpoints (prospects group, canonical `zE_czJ22`) are on v1 branch
  2. The `process_pending_prospects` scheduled task (ID 575 on dev) calls the v1 API endpoint via `api.request`
  3. MCP has no "run function" capability
- **Conclusion**: Dev deployment is verified via code comparison. Runtime testing requires merging to v1 or creating a dev-branch API endpoint.

## Next Steps

1. **Merge to v1**: Merge dev function to v1 in Xano UI (or update v1 function ID 11280 via MCP)
2. **Reprocess prospect 72100**: Reset `parse_status` to `"pending"` and trigger reprocessing on v1
3. **Monitor**: Check Xano logs for `DBO::set()` type errors after v1 deployment
