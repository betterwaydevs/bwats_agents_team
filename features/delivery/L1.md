# L1: Delivery Log

## PM: Assignment
- **Status**: done
- **Agent**: project-manager
- **Date**: 2026-02-27
- **Notes**: VM management page — Kamatera VMs with power controls and RDP access. Backend API + Frontend page. Phase 2 added RDP dropdown with Copy IP, Copy RDP URI, and help section.

## DEV: Backend
- **Status**: done
- **Agent**: backend-developer
- **Date**: 2026-02-27
- **Notes**:
  - Created `virtual_machines` API group (canonical: `6esnlNbi`) with 3 endpoints:
    - `GET /servers` (ID: 43107) — Lists all Kamatera VMs, enriched with CPU, RAM, IP from detail calls
    - `GET /server/{server_id}` (ID: 43108) — Single VM details
    - `PUT /server/{server_id}/power` (ID: 43109) — Power on/off/restart
  - All endpoints require BWATS JWT auth
  - Auth headers: `AuthClientId` + `AuthSecret`
  - Kamatera credentials from Xano env vars: `kamatera_access_key`, `kamatera_secret`
  - Tailscale integration: enriches VM list with Tailscale IPs from Tailscale API
- **Commits**: bwats_xano@f208d49, bwats_xano@3d18bee, bwats_xano@2e1ad3b

## DEV: Frontend
- **Status**: done
- **Agent**: frontend-developer
- **Date**: 2026-02-27
- **Notes**:
  - `src/services/virtualMachineApi.ts` — API service for VM endpoints
  - `src/apps/bwats/pages/BwatsVirtualMachines.tsx` — Full VM management page:
    - VM table: name, status badge (green Running / red Stopped), datacenter, CPU, RAM, public IP, Tailscale IP
    - Power controls: Start/Stop/Restart with confirmation dialogs
    - RDP access: two standalone action buttons in actions column
      - **".rdp" button** — generates and downloads .rdp file with VM IP + username:s:Administrator
      - **"RDP URI" button** — copies `rdp://` URI with username for macOS Microsoft Remote Desktop
    - Inline copy icon buttons: next to each IP address in table cells (public IP and Tailscale IP)
      - Clicking copies the IP to clipboard, shows confirmation in toast
    - Toast messages include "Username: Administrator" reminder
    - Clipboard implementation: synchronous textarea + execCommand('copy') pattern for reliability
    - Auto-refresh every 30s, loading skeletons, error states
  - Route: `/virtual-machines` (ProtectedRoute + FullAccessRoute)
  - Nav: "Virtual Machines" in Others dropdown (Monitor icon)
- **Commits**: nearshore-talent-compass@e4e3cea, nearshore-talent-compass@c11f926, nearshore-talent-compass@e39217d

## QA: Testing
- **Status**: done
- **Agent**: qa-tester
- **Date**: 2026-02-27
- **Notes**:
  - Playwright E2E test at `tests/virtual-machines.spec.ts` — 8/8 PASS (45.9s)
  - Tests cover: page load, table data, standalone RDP buttons, inline IP copy buttons, power controls, navigation, refresh
  - TypeScript compiles clean (`npx tsc --noEmit`)
- **Screenshots**:
  - vm-page-loaded.png
  - vm-table-data.png
  - vm-rdp-buttons.png (standalone .rdp and RDP URI buttons)
  - vm-ip-copy-buttons.png (inline copy buttons next to IPs)
  - vm-power-controls.png
  - vm-nav-dropdown.png
  - vm-refresh-button.png

## DEV: Live Deployment
- **Status**: done
- **Agent**: backend-developer
- **Date**: 2026-02-27
- **Notes**:
  - Recreated `virtual_machines` API group on v1 branch via MCP (Xano UI merge not available programmatically)
  - v1 canonical: `3Bq6OWvc` (different from dev `6esnlNbi` — created independently, not merged)
  - All 4 endpoints deployed: GET /servers, GET /server/{server_id}, PUT /server/{server_id}/power, GET /tailscale_devices
  - Re-published all endpoints with `publish: true` to force function stack recompilation (initial MCP create did not fully compile Tailscale logic)
  - Frontend updated with environment-aware canonical: dev uses `6esnlNbi`, live uses `3Bq6OWvc`
  - Frontend commit pushed to main: `3c693f6`
  - Fixed uncompiled function stack on v1 — XanoScript was null, re-pushed with publish:true
  - Fixed Tailscale response path bug: `resp.response.devices` → `resp.response.result.devices`
  - Verified working: pablo-linkedin shows tailscale_ip 100.71.23.98 on live

## PO: Acceptance
- **Status**: done
- **Agent**: product-owner
- **Date**: 2026-02-27
- **Notes**: All features verified: .rdp download, Copy IP inline, RDP URI copy, help tooltip with username and platform instructions. User approved.

## User: Approval
- **Status**: done
- **Date**: 2026-02-27
- **Notes**: Approved. Deployed with standalone RDP buttons, inline IP copy, and help tooltip.
