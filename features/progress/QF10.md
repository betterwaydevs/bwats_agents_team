# QF10: Prospect Parser — Local Codex Processing

## Status: dev-complete (pending Add_prospect cutover)

---

## 2026-03-06 — Session Complete

### What's Done

**Track A — Xano APIs (v1, prospects group `zE_czJ22`, ID=1163)**
- `GET /get_for_parsing` — API ID 45573 — fetches + locks pending prospect
- `POST /save_parsed` — API ID 45574 — receives Codex JSON, normalizes, saves to DB + ES, triggers router
- `POST /mark_parse_status` — API ID 45575 — sets parse_status on any prospect
- All 3 tested and working. Auth: `N8N_WEBHOOK_TOKEN` env var.

**Track B — Python Parser Server**
- Location: `/home/pablo/projects/bwats/prospect_parser/`
- `bwats-parser` systemd service: active, running on port 8585
- Cron: runs `python3 parser.py --cron` every minute via user crontab
- 34/34 tests passing
- Codex model: `gpt-5.3-codex` via `codex exec --ephemeral`
- Xano scheduled task #576 (v1): **disabled** (schedule cleared)

**Bugs fixed (during E2E)**
- `xano_client.py`: handles `{found: false}` response → returns None
- `xano_client.py`: added `mark_status(prospect_id, status)` method
- `parse_core.py`: guards against empty HTML (< 50 chars) → returns NONE_FOUND
- `parse_core.py`: checks `prospect.get('found')` before accessing `prospect['id']`

**E2E Report**: `features/reports/QF10/qf10-e2e-test-report.md`

---

## NEXT TASK: Modify Add_prospect on v1 to Stop OpenAI Parsing

### What needs to happen

`Add_prospect` (v1 API ID 42827, path `GET /Add_prospect`) currently calls `parse_unparsed_prospect` which triggers OpenAI. This must be changed so new prospects are saved with `parse_status = "pending"` and returned fast — the local Codex parser will pick them up.

**Goal**: When extension calls `Add_prospect`, prospect is saved to DB with `parse_status = "pending"`, NO OpenAI call, returns success in < 2s.

### How to resume

Spawn a `backend-developer` agent and tell it:

> Read the Add_prospect endpoint on v1 (workspace 6, apigroup 1163, api_id 42827) via MCP `getAPI`. Also read parse_unparsed_prospect function to understand the full flow. Then modify Add_prospect to:
> 1. Keep all current inputs (auth_token, linked_html, linkedin_profile, etc.)
> 2. Save the prospect to DB with parse_status = "pending"
> 3. Remove the call to parse_unparsed_prospect / call_open_ia
> 4. Return a fast success response
>
> Deploy to v1. Test by calling:
> `curl -s "https://xano.atlanticsoft.co/api:zE_czJ22/Add_prospect?auth_token=AYfIMbL7GfrUs7CpTS8tZ6dDIs9h5bOGu6h32dZxA2kSe5o2G170egBlVUscvorf"`
>
> It should return fast without triggering any OpenAI calls. Then verify a prospect has parse_status = "pending" in the DB. The local Codex parser at `http://localhost:8585/parse` will pick it up within 60 seconds.

### Reference files
- Dev version: `/home/pablo/projects/bwats/bwats_xano/apis/prospects/42291_Add_prospect_GET.xs`
- Dev version calls `parse_unparsed_prospect` — the cut point is removing that call
- `parse_unparsed_prospect` function: look up in `/home/pablo/projects/bwats/bwats_xano/functions/`
- N8N_WEBHOOK_TOKEN for testing: `AYfIMbL7GfrUs7CpTS8tZ6dDIs9h5bOGu6h32dZxA2kSe5o2G170egBlVUscvorf`
- Xano base URL: `https://xano.atlanticsoft.co`
